// Your input will be strings, in fact,
// all the strings that are defined in
// the system’s dictionary. This is
// located in /usr/share/dict/words

#include <cstdint>
#include <boost/math/distributions/chi_squared.hpp>

uint16_t x; // x has exactly 16 bits, and is unsigned

int main()
{
    return 0;
}